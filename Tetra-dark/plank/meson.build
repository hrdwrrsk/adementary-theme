plank_dir = join_paths(theme_dir_dark, 'plank')

# static theme files to copy
plank_theme_sources = [
  'dock.theme',
]

# Install theme files
install_data(plank_theme_sources, install_dir: plank_dir)
