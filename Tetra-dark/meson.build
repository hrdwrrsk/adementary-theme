name = 'Tetra-dark'
theme_dir_dark = join_paths(gtk_dark_theme_dir, name)

# static asset files to copy
index_sources = [
  'index.theme',
]

# Install index file
install_data(index_sources, install_dir: theme_dir_dark)

subdir('gtk-2.0')
subdir('gtk-3.0')
subdir('plank')
subdir('xfwm4')
